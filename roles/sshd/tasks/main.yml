---
- name: Enable sshd
  become: yes
  systemd:
    name: sshd
    enabled: yes
    state: started

- name: Authorize keys
  authorized_key:
    user: "{{ user }}"
    state: present
    key: "{{ item }}"
  with_file:
    - personal.pub
    - work.pub
    - ipad.pub
    - iphone.pub
